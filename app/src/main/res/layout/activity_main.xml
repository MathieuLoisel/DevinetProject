<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activity.MainActivity">

    <Toolbar
        android:id="@+id/tb_main"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        android:title="Devinet"
        />

    <Button
    android:id="@+id/btn_main_play"
    android:layout_width="200dp"
    android:layout_height="wrap_content"
    android:text="Jouer"
    app:layout_constraintLeft_toLeftOf="parent"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintBottom_toTopOf="@id/btn_main_submit"
    android:paddingTop="50dp"
    android:paddingBottom="50dp"
    android:onClick="onClickPlay"
    />

    <Button
        android:id="@+id/btn_main_submit"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:text="Proposer"
        app:layout_constraintTop_toBottomOf="@id/btn_main_play"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:paddingTop="50dp"
        android:paddingBottom="50dp"
        android:onClick="onClickSubmit"
        />

    <Button
        android:id="@+id/btn_main_my_results"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:text="Mes résultats"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/btn_main_submit"
        android:paddingTop="50dp"
        android:paddingBottom="50dp"
        android:onClick="onClickSubmit"
        />

    <Button
        android:id="@+id/btn_main_quit"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:text="Quitter"
        app:layout_constraintTop_toBottomOf="@id/btn_main_play"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:paddingTop="50dp"
        android:paddingBottom="50dp"
        android:onClick="onClickQuit"
        />


</androidx.constraintlayout.widget.ConstraintLayout>