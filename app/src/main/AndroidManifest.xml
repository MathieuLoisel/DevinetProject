<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.devinetproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.PlayActivity"></activity>
        <activity android:name=".activity.FindTheWordActivity" />
        <activity android:name=".activity.SelectListActivity" />
        <activity android:name=".activity.SelectLevelActivity" />
        <activity android:name=".activity.MyResultsDetailsActivity" />
        <activity android:name=".activity.MyResultsActivity" />
        <activity android:name=".activity.MyResultsGeneralActivity" />
        <activity android:name=".activity.SubmitActivity" />
        <activity android:name=".activity.SettingsActivity" />
        <activity android:name=".activity.AboutUsActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>